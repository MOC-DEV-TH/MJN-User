import 'package:flutter/material.dart';

class AccountScreen extends StatelessWidget {
  static const routeName = '/account_screen';

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Account Screen'),);
  }
}
